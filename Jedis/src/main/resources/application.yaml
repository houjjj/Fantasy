spring:
  redis:
    host: 10.2.38.221
    port: 6379
    password:
    jedis:
      pool:
        max-active: 200
        max-wait: -1
        max-idle: 10
        min-idle: 0
    database: 12

management:
  health:
    redis:
      enabled: false
logging:
  level:
    root:
      DEBUG

#debug:
#  true
