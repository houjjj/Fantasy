<!--
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.houjun.mybatis.mapper.HRMapper">
&lt;!&ndash;  <cache></cache>&ndash;&gt;
  <select id="selectById" parameterType="int" resultType="com.houjun.mybatis.domain.HR">
    select * from hr where id=#{id}
  </select>
  <update id="updateName" >
    update hr set name=#{name} where id=#{id}
  </update>
</mapper>-->
