spring:
  redis:
    host: 192.102.26.93
    port: 32147
    password: BlockchainTest@redis0312
    jedis:
      pool:
        max-active: 200
        max-wait: -1
        max-idle: 10
        min-idle: 0
    database: 12

management:
  health:
    redis:
      enabled: false
